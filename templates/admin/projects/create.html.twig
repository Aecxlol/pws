{% extends 'admin/layouts/base.html.twig' %}

{% block body %}
    <div class="admin-sub-title">
        <h2 class="white padding-30">{{ currentPage }}</h2>
    </div>

    <div class="admin-sub-content">
        <h3 class="white">Ajouter un projet</h3>
        {{ include('/admin/partials/_user_create_update_form.html.twig', { btnTextContent: 'Ajouter' }) }}
        <div>
            <img id="image" src="{{ asset('img/logo/logo.jpg') }}">
        </div>
    </div>
{% endblock %}