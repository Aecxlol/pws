{% extends 'admin/layouts/base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}
    <div class="admin-sub-title">
        <h2 class="white padding-30">{{ currentPage }}</h2>
    </div>

    <div class="admin-sub-content">
        <h3 class="white">Ajouter une compétence</h3>
        {{ include('/admin/partials/_form.html.twig', { src: 'skills', btnTextContent: 'Ajouter' }) }}
    </div>
{% endblock %}
