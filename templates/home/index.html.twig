{% extends 'layouts/base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    {{ dump(app) }}
    {# MENU #}
    {{ include('/partials/_menu.html.twig') }}

    {# MAIN SECTION #}
    {{ include('/partials/_main_section.html.twig') }}

    {# ABOUT SECTION #}
    {{ include('/partials/_about_section.html.twig') }}

    {# FOOTER #}
    {{ include('/partials/_footer.html.twig') }}
{% endblock %}
